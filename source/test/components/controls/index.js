
import test from 'tape';

// import createcontrols from 'components/sontrols';

// const Controls = createControls(React);
test('Controls', nest => {
  nest.test('...with no parameters', assert => {
    const actual = '';
    const expected = '';
    const msg = 'ok';
    assert.equal(actual, expected, msg);

    assert.end();
  });
});
